services:
  web:
    build: .
    restart: unless-stopped

    ports:
      - "127.0.0.1:8000:8000"

    env_file:
      - .env

    volumes:
      - static_data:/app/staticfiles
      - media_data:/app/media

volumes:
  static_data:
  media_data:
